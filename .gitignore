# Bỏ qua các file rác của Python
__pycache__/
*.py[cod]
*$py.class

# Bỏ qua môi trường ảo (nếu có)
venv/
.venv/
env/

# Bỏ qua file biến môi trường (chứa mật khẩu...)
.env

# Bỏ qua file hệ thống
.DS_Store

# --- LƯU Ý VỀ DATABASE ---
# Bình thường người ta sẽ ignore file .db (để *.db ở đây).
# NHƯNG vì bạn muốn bạn kia có sẵn dữ liệu để chụp ảnh báo cáo,
# nên ĐỪNG thêm *.db vào đây nhé. Để trống dòng này.